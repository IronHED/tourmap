import React from 'react'
import { GoogleMap,useLoadScript,Marker,InfoWindow} from "@react-google-maps/api";
import { formatRelative } from "date-fns";

import usePlaceAutocomplete, {\geocode,
get}

const BearDrop = () => {
    return (
        <div>
            map
        </div>
    )
}

export default BearDrop
